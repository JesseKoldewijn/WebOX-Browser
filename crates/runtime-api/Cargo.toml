[package]
name = "webox-runtime-api"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
webox-config = { path = "../config", version = "0.1.0" }
webox-engine = { path = "../engine", version = "0.1.0" }
webox-memory = { path = "../memory", version = "0.1.0" }
