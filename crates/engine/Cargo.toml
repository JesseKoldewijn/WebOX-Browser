[package]
name = "webox-engine"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
cef = { version = "147.1.0", default-features = false, features = ["linux-x11"] }
webox-config = { path = "../config", version = "0.1.0" }
