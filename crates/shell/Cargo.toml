[package]
name = "webox-shell"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
webox-config = { path = "../config", version = "0.1.1" }
webox-engine = { path = "../engine", version = "0.1.1" }
webox-memory = { path = "../memory", version = "0.1.1" }
webox-ui     = { path = "../ui", version = "0.1.1" }
